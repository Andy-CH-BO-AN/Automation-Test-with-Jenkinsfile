pipeline {
    agent any
    triggers {
        // 看要用定時 trigger 還是用 pr trigger
    }

    environment {
        // set up your jenkins credentials
    }


    stages {
        // set up your stages
        }
    }
}

